{
  "name": "ai-wildlife-antipoaching-ops",
  "version": "1.0.0",
  "description": "AI-powered wildlife conservation operations platform (ranger patrols, camera traps, snare hotspots, poacher incidents, drone surveillance)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
